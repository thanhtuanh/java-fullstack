name: CI/CD for Book API with Docker

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: 🔄 Checkout Code
      uses: actions/checkout@v3

    - name: ☕ Set up JDK 17
      uses: actions/setup-java@v3
      with:
        java-version: '17'
        distribution: 'temurin'

    - name: 🧪 Build & Test with Maven
      run: mvn clean package

    - name: 🐳 Build Docker Image
      run: docker build -t book-api:latest .

    # Optional: DockerHub Deployment (wenn du willst)
    # - name: 🔐 Login to DockerHub
    #   uses: docker/login-action@v3
    #   with:
    #     username: ${{ secrets.DOCKER_USERNAME }}
    #     password: ${{ secrets.DOCKER_PASSWORD }}

    # - name: 📤 Push Image to DockerHub
    #   run: |
    #     docker tag book-api:latest ${{ secrets.DOCKER_USERNAME }}/book-api:latest
    #     docker push ${{ secrets.DOCKER_USERNAME }}/book-api:latest
